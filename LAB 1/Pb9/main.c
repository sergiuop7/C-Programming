#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a=0, b=0, c=0;
    printf("Dati cele 3 numere: \n");
    scanf("%d %d %d", &a, &b, &c);
    printf("The product of %d, %d, %d= %d\n", a, b, c, a*b*c);
    return 0;


}
